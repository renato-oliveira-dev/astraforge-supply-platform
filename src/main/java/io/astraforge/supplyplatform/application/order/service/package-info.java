/**
 * Stateless application services implementing order input ports.
 *
 * <p>Services load aggregates through output ports, execute domain
 * behavior, and persist the resulting state.</p>
 */
package io.astraforge.supplyplatform.application.order.service;
